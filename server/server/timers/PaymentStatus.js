const EventEmitter = require('events');

const PaymentStatusFunctions = async (timerDatas) => {
    // PRIVATE FUNCTIONS

    const __getAllTimers = () => {
        
    }
}

module.exports = PaymentStatusFunctions;